# Misc

Parts that are not (yet) assigned to a specific Cub configuration
(`cub/`, `cubxl/`, `cubxl_plus/`). When a part here is confirmed to
belong to a configuration, move it into that tree and give it a README.

| Part | Notes |
| --- | --- |
| `PAW-V2-Opentrons Temperature Module Mount/` | PAW-V2 (OT-2 era) temperature module mount. |
| `Para-Dox Well Plate Vial Cover_REV. 0.step` | Well plate vial cover. |
| `standard_96_position_photoredox_block_vial_retainer/` | Open-access vial retainer + gasket for the standard 96-position photoredox block; includes the `ursa_101960_retainer.py` parametric generator. |
